CRUD

/*
varying float hudRedF;
varying float hudGreenF;
varying float hudBlueF;
varying float hudAlphaF;

void main(void)
{
	gl_FragColor = vec4(hudRedF, hudGreenF, hudBlueF, hudAlphaF);

#ifdef FALSE_COLOR
gl_FragColor = vec4(3.0,3.0,3.0,3.0)/32.0;
#endif
}
*/